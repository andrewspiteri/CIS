---
applyTo: "src/Cis.Modules.SolutionDesign/**/*.cs"
---

# CIS SolutionDesign C# component

Component root: `src/Cis.Modules.SolutionDesign`.

- Keep Markdown canonical and `.cis/local/` derived.
- Preserve atomic lifecycle and approval parity across the overall design and component sheet.
- Fail closed on identity collisions, stale technical intent, or bundle-digest mismatch.
- Keep generation deterministic and idempotent; preserve human-authored content outside managed blocks.
