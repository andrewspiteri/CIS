title = "Change Impact Studio secret-scanning policy"

[[allowlists]]
description = "Context redaction regression history contains a synthetic AWS-shaped value that is never a credential."
regexTarget = "match"
regexes = [
  '''AKIA1234567890ABCDEF'''
]
